NovaCore Native - separate project files

This project is an Android ARM64 native-library starter.
It builds a shared library named libNovaCore.so.

It does not inject into or bypass another application's protections.

Build with Android Studio/Gradle and an Android NDK installed 
So Please Extract The File once You Import it Into Animal Company.
