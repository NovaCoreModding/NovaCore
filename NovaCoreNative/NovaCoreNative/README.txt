NovaCoreNative
==============

Android ARM64 native library project.

The C++ library builds as:
    libNovaCore.so

The project is an ordinary Android app that loads the native library. It does not inject into, patch, or bypass another application's protections.

Open the folder in Android Studio with the Android NDK installed, then build the app for arm64-v8a.
